// Variables passed into builtin named arguments
const port = 8080;
const file_descriptor = "index.html";
const is_project = false;

start_server(port: port, file_descriptor: file_descriptor, is_project: is_project);
